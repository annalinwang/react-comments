import React, {Component, useState} from 'react'

const App = () => {
      const [posts, setPosts] = useState('')
      changeVote = () => {
      //... makes a copy of that value
      const postsCopy = [...posts, newPosts]
      //access posts through indexing
      newPosts[0].vote = asldkfj
      setsPosts(newPosts)
    }
  }

export default App
  